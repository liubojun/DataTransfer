#子目录过滤

#规则一
#路径设置支持收集指定时间的目录
#配置参数：%T-/+NF %t%Y %t%m %t%d %t%H %t%M %t%S
#%T : 表示执行操作
#-/+ : 可选择 -（减法）+（加法）
#N :  整数值
#F :  可选择Y(年)/m(月)/d(日)/H(时)/M(分)/S(秒)
#%Y: 年  %m: 月  %d: 日  %H: 时 %M: 分 %S: 秒
#%t: 分隔符
#当收集目录包含此配置时（如：%T-1d%t%Y%t%m%t%d将收集目录中的天数减去一天），收集传输软件将在收集此目录时将根据配置在Y(年) 或m(月) 或 d(日) 或 h(时) 或 M(分) 或 s(秒)加上或减去指定N的数字。


#规则二
#支持按照正则表达式匹配

#规则三
#支持时间以及正则表达式组合
[NULL]

[amcforcast]
level1=%T-[0,12,14]H%t%Y%t%m%t%d%t%Hf.*
level2=00.*